# .env.example

# The base URL of your backend API (update for production/development as needed)
REACT_APP_API_URL=http://localhost:5000

# The port the frontend should run on (optional, defaults to 3000)
PORT=3000